name: S3 Backend Integration
on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "integration/s3/**"
      - "integration/fixtures/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "Dockerfile"
      - ".github/workflows/integration-s3.yml"
  pull_request:
    paths:
      - "src/**"
      - "integration/s3/**"
      - "integration/fixtures/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "Dockerfile"
      - ".github/workflows/integration-s3.yml"

concurrency:
  group: integration-s3-${{ github.ref }}
  cancel-in-progress: true

jobs:
  s3-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build truss image (with S3 feature) with cache
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          load: true
          tags: truss:local
          cache-from: type=gha,scope=integration-s3
          cache-to: type=gha,mode=max,scope=integration-s3

      - name: Run S3 integration tests
        run: |
          cd integration/s3
          docker compose up --no-build --abort-on-container-exit --exit-code-from runn
        timeout-minutes: 10
        env:
          TRUSS_IMAGE: truss:local

      - name: Cleanup
        if: always()
        run: |
          cd integration/s3
          docker compose down --volumes --remove-orphans
